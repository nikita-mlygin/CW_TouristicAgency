<UserControl x:Class="NCourseWork.User.UserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NCourseWork.User" 
             d:DataContext="{d:DesignInstance Type=local:UserViewModel}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Width="400" VerticalAlignment="Center" HorizontalAlignment="Center">
            <StackPanel>
                <TextBlock Text="Login:" />
                <TextBox Text="{Binding Login}" />
            </StackPanel>
            <StackPanel>
                <TextBlock Text="Password:" />
                <TextBox Text="{Binding Password}" />
            </StackPanel>
            <Button Width="200" Height="40" Command="{Binding TryLoginCommand}" Content="Login" />
        </StackPanel>
    </Grid>
</UserControl>
